import { Navbar } from "./NavBar";
import { Outlet, Link } from "react-router-dom";

export const About = () => {
    return (
        <>
        <NavBar />
        <h1>About us</h1>
        </>
    );
}